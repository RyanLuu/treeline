#pragma once

#include <cmath>

struct CTranslation {
    float x;  // pixels
    float y;  // pixels
};

struct CVelocity {
    float vx;  // pixels per second
    float vy;  // pixels per second
};

